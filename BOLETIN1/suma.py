l = [10,13,16] 

def suma(list) :
    sum = 0
    for i in range(len(list)) :
        sum += list[i]
    print(sum) 

def otraSuma(list) :
    sum = 0
    for num in list:
        sum += num
    print(sum)
